group 'org.jim'
version '1.2.1'

apply plugin: 'java'

sourceCompatibility = 1.7
targetCompatibility = 1.7

repositories {
    //mavenCentral()
}

dependencies {
    // 编译时请手动添加jar包: spigot1.9, Multiverse-Core
    compile fileTree(dir: 'libs', include: '*.jar')
}


tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}